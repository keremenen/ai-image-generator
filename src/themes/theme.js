import { createTheme } from '@mui/material'

export const theme = createTheme({
	palette: {
		mode: 'dark',
		background: {
			paper: '#0f1016',
			default: '#181B24',
		},
		text: {
			primary: '#fff',
		},
		primary: {
			main: '#bebebe',
		},
	},
})

export default theme
